// main function
fn main() {
  // Print some stuff to console
  // println! is a macro, what exactly is a macro? like C macros?
  println!("Hello"); 
  println!("World");
}
// To run,"$ rustc hello.rs" "$ ./hello"
// By default creates 5.5MB hello executeable.
// There is probably a way to create a slimmed down version

// Initial feelings: It seems consise, tendency to use sigils.
